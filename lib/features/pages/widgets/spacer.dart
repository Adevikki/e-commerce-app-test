import 'package:flutter/material.dart';

//creates a vertical sizedbox spacing
Widget vertical(double value) => SizedBox(
      height: value,
    );
//creates a horizontal sizedbox spacing
Widget horizontal(double value) => SizedBox(
      width: value,
    );
